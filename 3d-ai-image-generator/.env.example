# Environment Variables for 3D AI Image Generator

# Node Environment (automatically set by Vercel)
NODE_ENV=production

# Optional: Custom API keys for image generation services
# (Currently using free services, but you can add paid services here)
# OPENAI_API_KEY=your_openai_key_here
# REPLICATE_API_TOKEN=your_replicate_token_here
# STABILITY_API_KEY=your_stability_key_here

# Optional: Custom domain for CORS (if needed)
# ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Optional: Maximum file size for downloads (in bytes)
# MAX_DOWNLOAD_SIZE=10485760

# Optional: Custom timeout for API requests (in milliseconds)
# API_TIMEOUT=30000

# Optional: Enable debug logging
# DEBUG=false
